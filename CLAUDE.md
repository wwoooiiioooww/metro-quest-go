# CLAUDE.md — Shotaのハーネス（作業フロー定義）

このリポジトリで作業するすべてのAIは、以下のフローと Skill を必ず守ること。

## 作業フロー（承認ゲートは2箇所のみ）

1. **計画提示 →【承認①】**
   着手前に必ず提示する: (a) 変更対象ファイルと変更概要 (b) 作業順序 (c) 完成の定義（DoD） (d) 設計判断が必要な点は「選択肢＋推奨＋理由」。
   承認を得るまで実装しない。
2. **実装＋自己検証**
   実装後、承認を求める前に自分で行う: 構文チェック（`node --check` 等）、既存テストの全実行、新機能の再発防止テスト追加、review-core のレビューチェックリストとの照合。
3. **成果報告 →【承認②: Go/NoGo】**
   報告は結論ファーストで: (a) DoDとの突き合わせ結果 (b) diffの要約 (c) テスト結果（`runtime errors: none` まで） (d) 未検証箇所の明記 (e) 判断してほしい点。
   ※NoGoや修正指示のうち「今後も同じ判断をしてほしいもの」は、該当Skillへの追記案を1行で添えること（ハーネスの改訂サイクル）。

## 必須Skill（このリポジトリ: 地下鉄クエスト）

| 対象 | 必須Skill |
|---|---|
| すべて（土台） | review-core |
| 地下鉄クエスト | + pwa-multiapp |

Skillは `.claude/skills/` に格納されている。原本は `wwoooiiioooww/skills-source` で管理し、変更はまず原本に反映してから各リポジトリへ複製する。

## 2リポジトリ同期のルール（このアプリ固有）

地下鉄クエストは2つのリポジトリで同じコードベースを共有している。

| リポジトリ | 位置づけ |
|---|---|
| `metro-quest` | 家族版。実名・現金報酬あり |
| `metro-quest-go` | 配布・提案版。個人情報ゼロ・現金なし・免責あり |

- **バグ修正は必ず両方に適用する。** 片方だけ直して終わりにしない。
- コピーしてよい箇所と、してはいけない箇所は **`metro-quest-go/SYNC.md` の差分表** で判断する。
- **新たに差分を作ったら、SYNC.md に1行足す。** 表に無い差分は次の同期で消える。

## 禁止事項

- 承認①なしの実装着手
- スコープ外の変更の混入（改善提案は分離して報告）
- ユーザーデータを壊す変更（migrateなしのスキーマ変更）
- 「直しました」だけの報告
- 片方のリポジトリだけへのバグ修正（上記「2リポジトリ同期のルール」参照）
