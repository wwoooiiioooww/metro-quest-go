# SYNC.md — 家族版との差分一覧

このリポジトリ（`metro-quest-go` / 配布・提案版）は、
[`metro-quest`](https://github.com/wwoooiiioooww/metro-quest)（家族版）と
**同じコードベース**です。

## バグ修正の手順

1. **どちらか一方で直す**
2. **もう一方にも同じ修正を入れる**
3. その際、**下の表に載っている箇所だけは、そのままコピーしてはいけない**

表に無い部分は、丸ごとコピーして構いません。

---

## 差分の全一覧

| # | 箇所 | 家族版 `metro-quest` | 配布版 `metro-quest-go` | 理由 |
|---|---|---|---|---|
| 1 | `APP_KEY` | `'mq_v1'` | `'mqgo_v1'` | GitHub Pagesでは両者が**同一オリジン**。キーが同じだとデータを共有してしまう |
| 2 | `CACHE_NAME` (sw.js) | `'metro-quest-v10'` | `'metro-quest-go-v16'` | 同上。Cache Storageもオリジン単位 |
| 3 | `APP_VER` | `'v11'` | `'v16'` | 別アプリとして採番。`CACHE_NAME` と必ず揃える |
| 4 | `migrateLegacy()` | **あり** | **なし（意図的）** | 配布版が旧キー（`questHistory` 等）を拾うと、**家族版のデータを取り込んでしまう** |
| 5 | `LEGACY_KEYS` | あり | なし | 同上 |
| 6 | `TARGET` | なし | `{ companies:['metro'], strict:false }` | 配布先ごとの初期路線の切り替え |
| 7 | `DEFAULT_SETTINGS.lines` | `["metro","toei"]` 固定 | `TARGET.companies` 参照 | 同上 |
| 8 | `DEFAULT_SETTINGS.rewardMode` | なし | `"stamp"` | 配布物に現金の要素を初期表示しないため |
| 9 | `DEFAULT_SETTINGS.stamps` | なし | あり | ⭐スタンプの抽選表 |
| 10 | `REWARD_MODES` / `rewardMode()` / `rewardDef()` / `rewardTable()` / `rewardValue()` | なし | あり | ごほうび種別の切替 |
| 11 | `DEFAULT_SETTINGS.targets` | `そら` / `ふう` / `パパ` / `ママ` / `元気兄弟` | `こども1` / `こども2` / `おとな1` / `おとな2` / `みんな` | 配布物に家族の情報を出さない |
| 12 | 特別演出の判定 | `target === "元気兄弟"` | `target === "みんな"` | 11に対応 |
| 13 | 最高ランク名 | `"👑 伝説の元気兄弟"` | `"👑 伝説のたんけん隊"` | 11に対応 |
| 14 | タイトル / h1 / manifest | `🎰 地下鉄クエスト・スロット` | `🚇 地下鉄クエストGO` | 遊技の記号を外す＋狭い画面で折り返さない長さに |
| 15 | ランク行の文言 | `スタンプ{n}個` | `たっせい{n}回` | ⭐スタンプ（ごほうび）と語が衝突するため |
| 16 | 合計表示 | `{n}円 💰` 固定 | モードに応じて `{n}こ ⭐` / `{n}円 💰` | 8に対応 |
| 17 | リール2のラベル | `金額` 固定 | `reel-label-2` をJSで書き換え | 8に対応 |
| 18 | 地蔵ボーナスの文言 | `元気兄弟それぞれに【+200円】` | `みんなに【+{モード別}】` | 8・11に対応 |
| 19 | 設定の「💰 金額」欄 | 金額のみ | 🎁 ごほうび（⭐/💰の切替つき） | 8に対応 |
| 20 | `loadSettings()` | 必須キーの検査のみ | 欠損キーをデフォルトで補完 | 項目追加に対して前方互換にするため |
| 21 | 「📖 はじめる前に」パネル | なし | あり（初回起動時に自動表示） | 配布物には安全上の注意と免責が必須 |
| 22 | `store.noticeSeen` | なし | あり | 21に対応 |
| 23 | `LICENSE` | なし | あり | 配布版のみ |
| 24 | `README.md` | 1行 | 事業者向けの確認表つき | 配布版のみ |
| 25 | `test/run-tests.mjs` | 移行テストあり（[2][3][4]） | 移行テストなし・GO固有テストあり | 4に対応 |
| 26 | アイコン | 元のデザイン | **地下鉄の先頭車両**（自作） | ホーム画面で見分けられるように |
| 27 | `manifest.json` の `name` | `地下鉄クエスト・スロット` | `地下鉄クエストGO` | Androidは `name` を表示するため |
| 28 | `ALLOW_MONEY_MODE` | なし | `true`（1行で `false` にできる） | 先方の指摘で現金要素を落とせるように |
| 29 | スタンプの抽選表 | なし | `⭐`〜`⭐⭐⭐⭐⭐`（1〜5） | ⭐の数で見せるため。20個並ぶと潰れる |
| 30 | `rewardValue()` | なし | 数字がなければ★⭐の数を数える | 29に対応 |
| 31 | `history[].mode` | なし | あり（`stamp` / `money`） | 今日の合計で★と円を足し合わせないため |
| 32 | `todayStats()` | 日付のみで集計 | 日付＋モードで集計 | 31に対応 |
| 33 | 画面下タブのアイコン | 🎰 あそぶ | 🚇 あそぶ | 遊技の記号を使わない |
| 34 | `legacyStampsV2()` / `isUnedited()` | なし | あり | 未編集の★表を⭐へ差し替えるため |
| 35 | `openNoticeFromSettings()` | なし | あり | 「はじめる前に」を設定から読み返すため |
| 36 | **あそぶタブの意匠** | リール＋STOPボタン | **運転台**（前面展望・速度計・LED方向幕・ブレーキレバー） | バッチ3b。GOで試してから家族版への移植を判断する |
| 37 | `setCab()` / `setCabSpeed()` / `cabState` | なし | あり | 36の状態機械（stopped/accel/cruise/braking/arriving） |
| 38 | `settings.motion` と `motionOn()` / `applyMotionPref()` | なし | あり | 演出OFF。`prefers-reduced-motion` にも追従 |
| 39 | 起動演出 `#splash` / `hideSplash()` | なし | あり | 1.2秒。演出OFFなら出さない |
| 40 | 運転台の音 | なし | 走行音・発車ベル・ブレーキ・到着 | 36に対応。走行音は oscillator を1本保持 |
| 41 | 注意書きの表示タイミング | — | **起動演出のあと** | 39に対応 |
| 42 | 景色 `SCENES` / `pickScene()` / `sceneKeyByClock()` | なし | **いまの時刻で決まる**（🌅あさ / ☀️ひる / 🌆ゆうがた / 🌙よる ＋ 🚇ちかてつを3回に1回） | 画面が勝手に暗くなることで「そろそろ帰ろう」を言いやすくする |
| 43 | ブレーキ | 各行にSTOPボタン×4 | **大きな1本**（`#brake-btn` / `pullBrake()`）で順番に確定 | 画面が縦に伸びてスクロールが必要だったため。運転台としても自然 |
| 44 | 表示器の出し分け `updateReelFocus()` | なし | あり（pending / active / done） | 43に対応。まだの項目は出さず、確定した項目は1行に畳む |
| 45 | お地蔵さんボーナス | メッセージのみ | **車窓の中で完結**（`#jizo` 大きく横切る ＋ `#jizo-banner` の帯 ＋ `#cab-flash` の閃光） | 下にスクロールしないと当たったことが分からなかった |
| 46 | 達成ボタン | 1回タップで確定 | **2回押し**（`confirmClear()` / `clearBtnReset()`） | 誤タップが多いため。4秒たつと解除される |
| 47 | 達成後の残り回数 | 減ったまま | **その日の回数がリセットされる** | 回数制限は「達成せず引き直し続ける」のを防ぐためのもの |
| 48 | `DEFAULT_PIN` / `isDefaultPin()` | なし | あり。**初期値のままなら、あいことばの入力画面そのものに「はじめは1234」と出す**（変更後は出さない） | 配ったばかりの人は1234を知らず、保護者モードに入れなかった |
| 49 | ヘッダー | 5ブロック（回数/合計/称号/版/注意書き） | 2行に圧縮。版と注意書きは せってい へ | 縦の圧縮 |
| 50 | ⚙️ボタン | あり | 削除（下タブと重複） | 縦の圧縮 |
| 51 | 車窓の描画 | なし | **canvasで一点透視**（`drawCab()` / `CAB` / `projX()` / `projY()`） | CSSのグラデーションでは「縞が動く」以上のことができなかった。物体に z を持たせて投影する |
| 52 | 操作系 | 発車ボタンのみ | **はっしゃ / ブレーキ の2つだけ**（左右に同大） | 警笛とドアは役割を持たせられなかったため削除 |
| 53 | 抽選の定位置 | 4行のリール | **行き先=車窓 / ごほうび=LED / 指令・だれ=下の2行** | 行き先が3か所に出ていた。それぞれに定位置を与えて重複をなくした |
| 54 | `BRAKE_ORDER` | なし | `[0,2,1,3]` | 止める順を 駅→ごほうび→指令→だれ に。番号自体は変えていない |
| 55 | 行き先の表示 | リール内 | `#dest-panel`（つぎは→とうちゃく＋路線カラーバッジ） | 53に対応 |
| 56 | `updateStartBtn()` | なし | あり | 回数ゼロのとき発車が無言で失敗していたため、状態を1か所で管理する |
| 57 | 表示器の回り方 | 発車で4つ同時に回る | **順番に1つずつ**（`spinReel()` / `coverReel()` / `REEL_COVER`）。まだの表示器は `- - -` で伏せる | 4つ同時だと目が疲れる。ブレーキが向いた先だけに注意が向くようにする |
| 58 | ブレーキの色 | （ブレーキボタン自体がない） | 押せる=明るいシアン／押せない=**はっしゃ と同じ灰色** | 「灰→灰」で押せるかどうかが分からなかった。はっしゃ の「黄→灰」と見え方をそろえる |
| 59 | ごほうびの累計 | `store.totalMoney`（数値1つ） | **`store.totals = { stamp, money }`** と `entryMode()` | ⭐と円は単位が違う。1つの数に足すと、モードを切り替えたとき「⭐55 + 100円 = 155円」と混ざった |
| 60 | `migrateTotals()` | なし | あり（古い `totalMoney` を履歴から数えなおす） | 59の移行。記録は間引かず、消すときは累計も0にしているので必ず復元できる |
| 61 | 記録の見た目 | 1行のテキスト | **きっぷ風カード**（`ticketHTML()`）。駅名を大きく、路線カラーの帯、切り取り線、ごほうびは赤いスタンプ | 毎日見るところ。集めたくなる形にする |
| 62 | 行った駅コレクション | 除外リストのみ | **`renderCollection()`**。路線ごとに駅チップを並べ、⭐=達成 / ✓=手で塗った で制覇率を出す | 「除外」という後ろ向きな見せ方を「12/142駅 いってきた!」に変える。路線図は使わない |
| 63 | まちあわせコード | なし | **`meetCodeOf()` / `stationByMeetCode()` / `joinMeetCode()`**。駅名から作る4文字 | 友だちと同じ駅にできる。そろえるのは**駅だけ**（指令・ごほうびは各家庭の設定のまま）。通信は一切しない |
| 64 | コードの作り方 | — | 駅名のハッシュ（並び順ではない）。全374駅で衝突0件をテストで固定 | 並び順だと、駅を1つ足しただけで既存コードが別の駅を指してしまう |

### 共通化された箇所（両方に同じものが入っている）

以下は**同じ実装**なので、片方を直したらもう片方にもそのまま反映してください。

- `unlockAudio()` … モバイルで音が鳴らない問題の対策
- `beep()` の音量 0.35（小さくて聞こえないとの報告への対応。両方に適用済み）
- 入力欄の `autocomplete="off"` … Androidの自動入力バー抑止
- `todayKey()` / 記録の `date` フィールド … 「今日の分」の集計
- 冒険日数の表示ロジック
- `RANKS` / `getRank()` / `nextRank()` / `openRanks()` … 称号のテーブル化と一覧表示
- `esc()` … ラベルに引用符やタグが入っても編集欄が壊れないようにする
- `updatePcts()` と重みエディタの%表示 … 「合計100が上限」という誤解の解消
- 入力欄の `<form autocomplete="off">` 包み・`name`属性・`parent-code` へのid変更
- **駅名の自由入力欄の廃止**（Androidの自動入力バーは属性では抑止できなかった。路線→駅チップの導線に一本化）
- **画面下タブ**（あそぶ/きろく/せってい）と `switchTab()` / `parentAuthed`
- **指令リストの行編集**（`renderMissionEditor()` / `addMissionRow()` / `delMission()`）
- **保護者モードのロック**（`lockParent()` と、背面移行時の自動ロック）
- **記録一覧の `esc()`**（指令に `<` や `"` を書いても一覧が壊れないようにする。両方に適用済み）

---

## 判断に迷ったときの原則

- **家族版**は「わが家で楽しいこと」が最優先。現金でも実名でもよい
- **配布版**は「知らない誰かに配っても大丈夫か」が最優先。個人情報ゼロ・現金なし・免責あり
- 迷ったら、**配布版はより保守的な側に倒す**

## 新しく差分を作ったときは

**必ずこの表に1行足してください。** ここに書かれていない差分は、次のバグ修正で確実に消えます。
